const uint16_t clockx = 120;                       // clock center X
const uint16_t clocky = 40;                        // clock center Y
const uint16_t len[3] = { 25, 30, 35 };            // Size of clock hands
const uint16_t colorhh = M5.Lcd.color565(251, 142, 53); // Color of clock hand(hour)
const uint16_t colormm = M5.Lcd.color565(251, 142, 53); // Color of clock hand(minute)
const uint16_t colorss = M5.Lcd.color565(255, 0, 0);    // Color of clock hand(second)
